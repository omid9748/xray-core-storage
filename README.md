## 🛠 راهنمای استفاده قدم به قدم (فقط در ۳ دقیقه)

### 1️⃣ مرحله اول: آماده‌سازی هسته Xray روی سرور

ابتدا با دسترسی `root` وارد ترمینال سرور اوبونتو خود شوید و پوشه کاری را بسازید:

```bash
mkdir -p /opt/xray
